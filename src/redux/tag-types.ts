export enum tagTypes {
    admin = 'admin',
    user = 'user',
    products= "products",
    employee= "employee",
    order= "order",
    cart= "cart"
 }
 
 export const tagTypesList = [
    tagTypes.admin,
    tagTypes.user,
    tagTypes.products,
    tagTypes.employee,
    tagTypes.order,
    tagTypes.cart
 ];