export enum tagTypes {
    admin = 'admin',
    user = 'user',
    products= "products",
    stores= "stores",
    menuItem= "menuItem",
    order= "order",
    cart= "cart"
 }
 
 export const tagTypesList = [
    tagTypes.admin,
    tagTypes.user,
    tagTypes.products,
    tagTypes.stores,
    tagTypes.menuItem,
    tagTypes.order,
    tagTypes.cart
 ];