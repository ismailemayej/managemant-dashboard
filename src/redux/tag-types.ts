export enum tagTypes {
    admin = 'admin',
    user = 'user',
    products= "products",
    employee= "employee",
    order= "order",
    customers = "customers",
    cart= "cart"
 }
 
 export const tagTypesList = [
    tagTypes.admin,
    tagTypes.user,
    tagTypes.products,
    tagTypes.employee,
    tagTypes.order,
    tagTypes.customers,
    tagTypes.cart
 ];
 